{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Least-privilege capability for the primary editor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
